export const environment = {
  production: true,
  // IMPORTANT: replace with your deployed backend's public URL before
  // building for production (e.g. Railway backend service URL + /api).
  // Example: 'https://smartschool-backend-production.up.railway.app/api'
  apiUrl: 'https://REPLACE_WITH_YOUR_BACKEND_URL/api',
};
